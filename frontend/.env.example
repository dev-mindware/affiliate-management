# SERVER-ONLY — nunca uses NEXT_PUBLIC_* para API URL ou secrets.
# O browser fala apenas com /api (BFF). Estas vars ficam no servidor Next.js.

API_URL=http://localhost:3333/api
MINDGEST_APP_URL=https://mindgest.mindware.ao
